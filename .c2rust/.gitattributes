# Configure language-aware diff drivers for Rust and C source files.
# This affects `git diff` output only; it does not influence pack delta compression.
*.rs  diff=rust
*.c   diff=c
*.h   diff=c

# Disable delta compression for binary/compiled artifacts to avoid meaningless pack bloat.
*.o   -delta
*.a   -delta
*.so  -delta
